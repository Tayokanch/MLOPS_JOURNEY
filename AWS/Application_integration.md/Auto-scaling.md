`AWS Application Auto Scaling`; This is typically used with `ELB`. 
   - Auto scaling automatically increases the number of server based on the amount of traffic with have on the queue. 
   - Once the traffic increase our server auto-scaled by adding more servers to handle our request instead of having to let traffic / request wait for long.
   - It also scale down the servers and soon as the traffic reduces