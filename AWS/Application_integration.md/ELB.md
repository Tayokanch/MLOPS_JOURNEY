`Elastic Load Balancing`:  This is used to direct traffic to their appropraite server. 
   -  It spreads load evenly accross multiple backend servers
   -  Unhealty servers are not available if failing
   -  Can be used with `EC2`, `ECS`, `EKS`, `Lambda` etc.
