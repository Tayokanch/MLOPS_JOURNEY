# Collections commmand

- `ansible-galaxy collection install collectionName(network.cisco)`: To install a cisco network collection. Once installed, we can utilized the modules and roles provided by the `Network.Cisco Collection` in our **Playbooks** This allow us to seamlessly integrare networ automatio taks specific to Cisco devices into our existing Ansible workflows

-