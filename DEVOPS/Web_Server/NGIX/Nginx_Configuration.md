# NGINX CONFIGURATION
- The main config file is typically name `nginx.conf` and is usually locvalted in the `/etc/nginx/` file directory

- Using a custom syntax comprising: `Directives`, `Blocks`
  
- `Directives`: This sets up the servers behavior i.e, this is where you can define whether you want your `Nginx` to to be a `web server`
- 